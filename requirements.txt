# Core dependencies — all version-pinned per audit-grade reproducibility commitment
 
# LLM SDKs
anthropic==0.42.0
voyageai==0.3.2
cohere==5.13.0
 
# Database
psycopg[binary]==3.2.3
pgvector==0.3.6
 
# Retrieval
rank-bm25==0.2.2
 
# HTML ingestion (Cornell LII fetch + parse for FDCPA chunker)
beautifulsoup4==4.12.3
requests==2.32.3
 
# Config and environment
python-dotenv==1.0.1
pyyaml==6.0.2
 